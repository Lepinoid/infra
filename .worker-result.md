# issue-4 作業結果 (自動更新基盤 クラスタ側 updater)

## 成果物

- **Draft PR**: https://github.com/Lepinoid/infra/pull/6 (issue-4 → main)
- **最終コミット**: `de96358 feat(build-server): Deployment 拡張 + RBAC/Lease/CronJob 追加と desired ConfigMap 整備` (計 27 コミット)

## スコープ完了状況 (In Scope A-J)

| 節 | 内容 | 状態 |
|---|---|---|
| A | Deployment 拡張 (initContainer plugin-updater-init / sidecar updater / POD_UID / OVERRIDE_SERVER_PROPERTIES=false / imagePullSecrets) | 完了 |
| B | 管理イメージ build/publish workflow (`.github/workflows/plugin-updater-image.yml`) | 完了 (verify stage 付き) |
| C | desired ConfigMap + dispatch workflow (`.github/workflows/update-lepinoid-tools.yml`, desired.json キー整合) | 完了 |
| D | 5 分周期 CronJob + Lease (plugin-updater, 60s/30s renew) + heartbeat | 完了 |
| E | Fencing (fencingGeneration/expectedPodUid, Lease+blocked 確認) | 完了 |
| F | 中断回復A0-A6/B0-B11 (phase 分岐, init-recover, probe 未完了→blocking) | 完了 (統合接続済み) |
| G | GC (保護世代 + 最新 2 件 retention) | 完了 (B11 に接続) |
| H | 状態モデル versioned (journal/current/recovery/probe/blocking + strict JSON) | 完了 |
| I | bootstrap (current 不在時の live SHA backfill) | 完了 |
| J | rescue runbook (build-server/README.md plugin-updater 説明) | 完了 |

## 検証

- `updater/`: `go test -race -shuffle=on -count=1 ./...` 12 パッケージ全パス、`go vet ./...` clean、`gofmt -l` 差分なし、CGO_ENABLED=0 build 成功
- Dockerfile: `docker build --target verify` で updater/oras/kubectl/mc-monitor/tar がイメージ内 PATH に存在しバージョン出力まで確認
- マニフェスト: `kubectl kustomize .` 成功 (CronJob/Lease/RoleBinding/Role/ServiceAccount を含む全リソース生成確認)
- 共有 fixture: `build-server/contract-fixtures/shared` が /tmp/lepinoid-tools-ref と diff なし

## 重要な確定仕様 (オーケストレーター判断で確定)

1. `OVERRIDE_SERVER_PROPERTIES=false` を Deployment env に恒久設定 (itzg が毎起動 white-list を false に戻すのを防ぐ必須対応)。
2. `runtimeWhitelistEnabled` の snapshot は Paper 1.21.1 vanilla に read-only コマンドがないため、gate CLOSED 確定後に `whitelist on` の応答分岐で同定 (probe は `journal/<txId>.probe` に先行永続化、未完了時は blocking record を発行)。B0 では persisted 値を proxy 初期値として記録し probe 結果で確定する。実出力 fixture は upstream CFR decompile + en_us.json 言語リソースで同定した Paper 1.21.1 vanilla 文字列。
3. `plugin-versions.yaml` ConfigMap の data キーは `desired.json` (JSON 1 行) に統一。dispatch workflow もこれに揃えた。
4. CronJob の POD identity: Lease holder は `JOB_UID` (CronJob pod UID)。`t.podUID` は label selector `app=build-server` で実際の build-server Pod を発見して使用。

## 残作業 (別 PR)

- `ghcr.io/lepinoid/plugin-updater:REPLACE_WITH_DIGEST` プレースホルダを、初回管理イメージ公開 workflow 実行後に確定 digest へ置き換える。
- kubeconform / jsonschema-validate は依存ツール未整備のため今回は kustomize build のみ (要検討: CI 側で別途導入)。
- 実クラスタ障害注入および全経路検証は kind 等で後続実施を推奨。

## 既知の注記

- 管理イメージ publish workflow は GHCR tag の immutable 保証のみで、別 publisher との競合を registry 強制ブロックするものではない (issue 本文の通り今回の受け入れ範囲)。
- init recovery の `server.properties` キー単位 CAS 競合防止は deployment で white-list= を固定しない方針 (CAS 結果と probe 結果で runtime/persisted を両立) に限定している。
