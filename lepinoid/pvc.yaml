apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lepinoid-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      # world-sync の staging/現行/展開領域のため拡張 (issue #30)
      storage: 2Gi
