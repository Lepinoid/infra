apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-livesync-bridge-egress
spec:
  podSelector:
    matchLabels:
      app: livesync-bridge
  policyTypes:
    - Egress
  egress:
    - {}
